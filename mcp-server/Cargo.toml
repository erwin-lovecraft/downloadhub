[package]
name = "downloadhub-mcp-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MCP server exposing the download queue to external AI agents (Phase 3), subject to explicit user approval in the running desktop app."

[[bin]]
name = "mcp-server"
path = "src/main.rs"

[dependencies]
anyhow = "1.0.100"
dotenvy = "0.15.7"
downloadhub-core = { path = "../core" }
rmcp = { version = "2.2.0", features = ["server", "macros", "transport-io"] }
schemars = "1.1.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros"] }
